org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configureondemand=true

android.useAndroidX=true
android.enableJetifier=true
android.nonTransitiveRClass=true
kotlin.code.style=official
android.enableR8.fullMode=false

# ── Shoplive private Maven / demo keys ──────────────────────────────────────
# Do not put credentials or keys in this file: it is committed.
# Put them in local.properties (not committed, recommended) or inject them through
# environment variables. See section 1 of README.md for the exact format.
