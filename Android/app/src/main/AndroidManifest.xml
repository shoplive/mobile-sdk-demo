<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- SDK 가 요구하는 권한. INTERNET·ACCESS_NETWORK_STATE·VIBRATE·CAMERA 는 AAR 매니페스트에
         이미 선언돼 있어 병합되지만, 명시해 두면 무엇이 필요한지 한눈에 보인다. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- 광고 ID 기반 어트리뷰션을 쓸 때만 필요 (선택) -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID" />

    <!-- Mission 8 송출용. RECORD_AUDIO 는 AAR 에 없으므로 앱이 반드시 선언해야 한다.
         런타임 요청은 ShopliveStreamer.start() 가 대신 해 준다. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <application
        android:name=".DemoApplication"
        android:allowBackup="false"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.ShopliveOnboarding">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/Theme.ShopliveOnboarding"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Mission 2 — 딥링크 진입점.
             shoplivedemo://live?campaign={CAMPAIGN_KEY}&ref={REFERRER}
             콜드 스타트에서도 안전하도록 이 화면은 파싱만 하고 MainActivity 로 넘긴다. -->
        <activity
            android:name=".SchemeActivity"
            android:exported="true"
            android:noHistory="true"
            android:theme="@style/Theme.ShopliveOnboarding.Transparent">
            <intent-filter android:autoVerify="false">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:host="live"
                    android:scheme="${deepLinkScheme}" />
            </intent-filter>
        </activity>

        <!-- Mission 6 — navigation(url) 요청의 도착지.
             SDK 플레이어 Activity 가 앞에 떠 있는 상태에서 열려야 하므로 화면(Activity)이다. -->
        <activity
            android:name=".ProductDetailActivity"
            android:exported="false"
            android:theme="@style/Theme.ShopliveOnboarding" />
    </application>
</manifest>
